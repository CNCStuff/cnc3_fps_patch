[fps_patch]

; Master switch. The dinput8 proxy still forwards normally when disabled.
enabled=1

; Supported values: 45, 60, 75, or 90. Default: 90.
target_fps=90

; Write fps_patch.log next to the DLL.
logging=1
